1. 배열 초기화시 [0]*100 과 [0 for i in range(100)] 은 차이가 없나? 깊은복사 얕은복사 같은 개념도 없나 ? for문을 사용하면 초기화가 O(N)이 되나?

2. SW 삼성 아카데미\파이썬 기초\ 리스트튜플\ 11.py 에서 보면 외부 요소를 가져다 쓰는 개념을 모르겠음.. 외부 변수, 전역변수 사용법

3. 파이썬에서 n&1에서 왜 n이 홀수이면 1이고 짝수이면 결과가 0이 되는가? 비트로 변경하면 1은 다른자리가 전부 0이므로 무조건 0이되고 홀수면 1의자리가 1, 짝수면 0이므로 ?

4. 내문서\알고리즘 학습\Linked_list에서 16번째줄, 주소를 객체 자체로 선언해주는 의미. 이게 어떻게 주소를 의미하게 되나. 해당 코드가 주피터는 되는데 vscode에서 안되는 이유

5. https://jayb-log.tistory.com/52 에서 배열을 선언할 때 2차원 배열을 * *로 선언하면 내용물이 다 바뀌어버린다. 얕은 복사라 그런거면 이 복사는 사용하면 안되는건가? 

6. https://www.withsg.com/58 의 코드에서 bfs 함수에서 강사님의 경우 변수를 넣을 때 항상 파라미터에 넣으셨는데 이 사람은 넣지 않았다.
  언제 전역변수를 쓰고 언제 이렇게 짜고, 언제 변수를 넣어주는지 구별이 안간다..

1. 배열을 초기화할 때 이차원 배열의 경우 [0]*100 과 [0 for i in range(100)]가 다른 결과를 나올 때가 있었습니다. 전자의 경우 어떤 요소를 바꿀 때 다른 요소도 전부 바뀔 때가 있던데 이게 얕은 복사 때문인가요?
전자를 사용하려면 1차원배열에서만 사용하는게 좋을까요?

2. https://www.withsg.com/58 의 코드를 보면 bfs를 풀 때 dist 리스트 같은 경우 전역변수 사용보다 파라미터로 넣어주셨던 것으로 아는데요, 이 사람의 경우는
아예 둘다 안하고 그냥 선언해서 사용하더라구요.. 이게 언제 파라미터를 쓰는지, 전역변수, 혹은 이렇게 아무것도 안해도 되는지 매우 헷갈립니다.. 
제가 이해하기로는 전역변수나 파라미터는 재귀처럼 계속 여러 겹으로 동작할 때 어느 구간에서든 그 해당 값을 변경하기 위해 사용하고 한 겹일때에는 그냥 함수가 출력되는 위치 위에다가 쓰면 되는것인가요?

