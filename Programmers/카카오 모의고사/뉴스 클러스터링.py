
# 구해야 하는 값 = 자카드 유사도
def J(set1, set2):
    # 넘겨받은 set를 이용해서 자카드 유사도를 구한다.

    # 교집합의 길이를 합집합의 길이로 나눈 결과를 출력한다.
    # 문자열의 길이가 최대 1000이므로 set의 최대 개수는 최대 999개이고 
    # O(N^2) 에도 1000000 이므로 충분히 커버 가능한 속도가 된다.
    
    return


def solution(str1, str2):
    # 입력받은 string 값을 이 함수에서 set로 만들어 J함수에 넘겨준다.
    
    return 